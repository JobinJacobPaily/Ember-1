import mathLib from 'my-first-app/utils/mathlib';
import { module, test } from 'qunit';

module('Unit | Utility | math-lib', function() {

  // Replace this with your real tests.
  test('it may works', function(assert) {
	 
    assert.strictEqual(mathLib(2) ,true);
  });
});
